import styles from "./getInTouchButton.module.scss"

export interface IGetInTouchButtonsProps {
  text: string
  icon: HTMLImageElement
}

const GetInTouchButtons = ({ text, icon }: IGetInTouchButtonsProps) => {
  return (
    <>
      <div></div>
    </>
  )
}

export default GetInTouchButtons
